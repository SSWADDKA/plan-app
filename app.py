import streamlit as st
import google.generativeai as genai
from docxtpl import DocxTemplate
import io
import re

# --- 1. ข้อมูลวิชาตามหลักสูตรไทย ---
SUBJECTS_DATA = {
    "ประถมศึกษา": ["ภาษาไทย", "คณิตศาสตร์", "วิทยาศาสตร์และเทคโนโลยี", "สังคมศึกษาฯ", "สุขศึกษาและพลศึกษา", "ศิลปะ", "การงานอาชีพ", "ภาษาอังกฤษ"],
    "มัธยมศึกษาตอนต้น": ["ภาษาไทยพื้นฐาน", "คณิตศาสตร์พื้นฐาน", "วิทยาศาสตร์พื้นฐาน", "วิทยาการคำนวณ", "สังคมศึกษาฯ", "ประวัติศาสตร์", "สุขศึกษา", "พลศึกษา", "ศิลปะ", "การงานอาชีพ", "ภาษาอังกฤษพื้นฐาน"],
    "มัธยมศึกษาตอนปลาย": ["ภาษาไทยพื้นฐาน", "คณิตศาสตร์พื้นฐาน", "คณิตศาสตร์เพิ่มเติม", "ฟิสิกส์", "เคมี", "ชีววิทยา", "โลกและอวกาศ", "สังคมศึกษาฯ", "สุขศึกษา", "พลศึกษา", "ศิลปะ", "ภาษาอังกฤษพื้นฐาน", "วิทยาการคำนวณ", "การออกแบบและเทคโนโลยี"]
}

KPA_OPTIONS = {
    "K (Knowledge)": ["ความรู้ความเข้าใจ", "การอธิบายหลักการ", "ความเข้าใจเนื้อหา", "การระบุองค์ประกอบ"],
    "P (Process)": ["ทักษะการปฏิบัติ", "ทักษะการคิดวิเคราะห์", "ทักษะการแก้ปัญหา", "ทักษะการทำงานกลุ่ม"],
    "A (Attitude)": ["ใฝ่เรียนรู้", "มุ่งมั่นในการทำงาน", "มีวินัย", "มีจิตสาธารณะ"]
}

# --- 2. การตกแต่ง UI ด้วย CSS (เน้นความสะอาดและเลเยอร์กระดาษ) ---
st.set_page_config(layout="wide", page_title="PLAN - ระบบช่วยเขียนแผนการสอนมืออาชีพ")

st.markdown("""
    <style>
    /* พื้นหลังแอป */
    .stApp { background-color: #f0f2f5; }
    
    /* Sidebar กว้างขึ้น */
    [data-testid="stSidebar"] { width: 500px !important; background-color: #ffffff; }

    /* หน้ากระดาษพรีวิว (Layer 1) */
    .paper-page {
        background-color: white;
        padding: 60px 80px;
        border: 1px solid #d1d1d1;
        box-shadow: 0 15px 35px rgba(0,0,0,0.1);
        color: black;
        font-family: 'Sarabun', sans-serif;
        line-height: 1.8;
    }

    /* หัวแผนการสอน */
    .plan-header {
        text-align: center;
        border-bottom: 2px solid black;
        margin-bottom: 30px;
        padding-bottom: 10px;
    }

    /* การจัดรูปแบบตารางในหน้า Preview */
    table {
        width: 100%;
        border-collapse: collapse;
        margin: 20px 0;
    }
    th, td {
        border: 1px solid #000;
        padding: 10px;
        text-align: left;
        font-size: 14px;
    }
    th { background-color: #f2f2f2; }

    /* ปุ่มกดสีดำ */
    div.stButton > button {
        width: 100%; border-radius: 8px; height: 3.5em; background-color: #000;
        color: #fff; font-weight: bold; border: none; transition: 0.3s;
    }
    div.stButton > button:hover { background-color: #333; transform: translateY(-2px); }
    </style>
    """, unsafe_allow_html=True)

# --- 3. ฟังก์ชันช่วยลบสัญลักษณ์ที่ไม่ต้องการ (Cleaning) ---
def clean_markdown(text):
    # ลบสัญลักษณ์ # (Headers), * (Bold/Italic), _ (Underline) และ ))
    text = re.sub(r'[#*_]{1,3}', '', text)
    text = text.replace('))', '').replace('((', '')
    return text.strip()

# --- 4. การเชื่อมต่อ API ---
GOOGLE_API_KEY = st.secrets["GOOGLE_API_KEY"]
genai.configure(api_key=GOOGLE_API_KEY)
model = genai.GenerativeModel('gemini-2.5-flash')

# --- 5. SIDEBAR: ส่วนกรอกข้อมูล ---
with st.sidebar:
    st.title("📝 ข้อมูลแผนการจัดการเรียนรู้")
    with st.form("input_form"):
        school_name = st.text_input("ชื่อโรงเรียน", placeholder="เช่น โรงเรียนวิทยาศาสตร์จุฬาภรณราชวิทยาลัย")
        teacher_name = st.text_input("ชื่อครูผู้สอน")
        
        grade_level = st.selectbox("ช่วงชั้น", list(SUBJECTS_DATA.keys()))
        selected_subject = st.selectbox("รายวิชา", SUBJECTS_DATA[grade_level])
        
        col1, col2 = st.columns(2)
        with col1:
            grade_spec = st.text_input("ชั้นปี (เช่น ม.3)")
            subject_code = st.text_input("รหัสวิชา")
        with col2:
            duration = st.text_input("เวลา (ชั่วโมง)", value="2")
            term_year = st.text_input("ภาคเรียน/ปีการศึกษา", value="1 / 2567")
            
        topic = st.text_input("หัวข้อบทเรียน")
        context_input = st.text_area("บริบทโรงเรียน/นักเรียน", height=80)
        
        # เพิ่มช่องทักษะแยกจาก KPA
        specific_skills = st.text_area("ทักษะเฉพาะที่ต้องการพัฒนา (เช่น ทักษะการคิดสร้างสรรค์)", height=80)
        
        st.markdown("**จุดประสงค์ตามมาตรฐาน (K-P-A)**")
        sel_k = st.multiselect("ด้านความรู้ (K)", KPA_OPTIONS["K (Knowledge)"])
        sel_p = st.multiselect("ด้านทักษะ (P)", KPA_OPTIONS["P (Process)"])
        sel_a = st.multiselect("ด้านเจตคติ (A)", KPA_OPTIONS["A (Attitude)"])
        
        submitted = st.form_submit_button("🚀 สร้างแผนการสอน")

# --- 6. ส่วนประมวลผล ---
if submitted:
    with st.spinner('AI กำลังร่างแผนการสอนและจัดทำตารางประเมิน...'):
        prompt = f"""
        จงเขียนแผนการจัดการเรียนรู้ภาษาไทยระดับมืออาชีพ โดยห้ามใช้สัญลักษณ์ตกแต่งเช่น ## หรือ ** ในเนื้อหา 
        ให้ใช้การขึ้นบรรทัดใหม่และหัวข้อที่ชัดเจนแทน
        
        รายละเอียด:
        โรงเรียน: {school_name}, วิชา: {selected_subject} ({subject_code}), ชั้น: {grade_spec}, เรื่อง: {topic}
        ครูผู้สอน: {teacher_name}, เวลา: {duration} ชม.
        ทักษะเฉพาะ: {specific_skills}
        KPA: K:{sel_k}, P:{sel_p}, A:{sel_a}
        
        โครงสร้างที่ต้องการ:
        1. สาระสำคัญ (Concept)
        2. จุดประสงค์การเรียนรู้ (K-P-A)
        3. กิจกรรมการเรียนรู้ (ขั้นนำ, สอน, สรุป)
        4. สื่อและแหล่งเรียนรู้
        5. ตารางเกณฑ์การประเมินรูบริกส์ (Rubric Score) โดยทำเป็นรูปแบบ Markdown Table 4 ระดับ (ดีเยี่ยม, ดี, พอใช้, ปรับปรุง)
        """
        response = model.generate_content(prompt)
        st.session_state['ai_raw'] = response.text
        st.session_state['meta'] = {
            'school': school_name, 'teacher': teacher_name, 'subject': selected_subject,
            'code': subject_code, 'grade': grade_spec, 'topic': topic, 'term': term_year, 'time': duration
        }

# --- 7. ส่วนแสดงผลพรีวิว (Paper View) ---
if 'ai_raw' in st.session_state:
    meta = st.session_state['meta']
    
    # ปุ่มดาวน์โหลด
    c_space, c_btn = st.columns([8, 2])
    with c_btn:
        try:
            doc = DocxTemplate("template.docx")
            doc.render({**meta, 'ai_content': clean_markdown(st.session_state['ai_raw'])})
            bio = io.BytesIO()
            doc.save(bio)
            st.download_button("📥 ดาวน์โหลด Word", bio.getvalue(), f"Plan_{meta['topic']}.docx")
        except: st.caption("⚠️ โปรดอัปโหลด template.docx")

    # ส่วน Preview หน้ากระดาษ
    st.markdown('<div class="paper-page">', unsafe_allow_html=True)
    
    # หัวแผนการสอน (จัดรูปแบบใหม่ให้สวยงาม)
    st.markdown(f"""
        <div class="plan-header">
            <h2 style="margin:0; color:black;">แผนการจัดการเรียนรู้</h2>
            <p style="margin:5px 0; font-size:18px;"><b>{meta['school']}</b></p>
            <p style="margin:2px 0;"><b>วิชา:</b> {meta['code']} {meta['subject']} | <b>ชั้น:</b> {meta['grade']} | <b>เวลา:</b> {meta['time']} ชั่วโมง</p>
            <p style="margin:2px 0;"><b>หน่วยการเรียนรู้เรื่อง:</b> {meta['topic']} | <b>ผู้สอน:</b> {meta['teacher']}</p>
        </div>
    """, unsafe_allow_html=True)

    # แสดงเนื้อหาแผน (ใช้ Markdown เพื่อให้ตารางแสดงผลได้ แต่ลบ Noise อื่นออก)
    clean_content = clean_markdown(st.session_state['ai_raw'])
    
    # จัดการส่วนหัวข้อให้เป็นตัวหนา (จำลอง)
    display_content = clean_content.replace("สาระสำคัญ", "**สาระสำคัญ**")\
                                   .replace("จุดประสงค์การเรียนรู้", "**จุดประสงค์การเรียนรู้**")\
                                   .replace("กิจกรรมการเรียนรู้", "**กิจกรรมการเรียนรู้**")\
                                   .replace("ตารางเกณฑ์การประเมินรูบริกส์", "**ตารางเกณฑ์การประเมินรูบริกส์**")
    
    # แสดงผลเนื้อหาและตาราง
    st.markdown(display_content)
    
    # ส่วนแก้ไขข้อมูล (เปิด-ปิดได้)
    with st.expander("🛠️ คลิกเพื่อแก้ไขเนื้อหาเพิ่มเติม"):
        edited = st.text_area("แก้ไขข้อความต้นฉบับ:", value=st.session_state['ai_raw'], height=400)
        st.session_state['ai_raw'] = edited

    st.markdown('</div>', unsafe_allow_html=True)         