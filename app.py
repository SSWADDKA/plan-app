import streamlit as st
import google.generativeai as genai
from docxtpl import DocxTemplate
import io
import re

# --- 1. ข้อมูลหลักสูตรและตัวเลือก KPA ---
SUBJECTS_DATA = {
    "ประถมศึกษา": ["ภาษาไทย", "คณิตศาสตร์", "วิทยาศาสตร์และเทคโนโลยี", "สังคมศึกษาฯ", "สุขศึกษาฯ", "ศิลปะ", "การงานอาชีพ", "ภาษาอังกฤษ"],
    "มัธยมศึกษาตอนต้น": ["ภาษาไทยพื้นฐาน", "คณิตศาสตร์พื้นฐาน", "วิทยาศาสตร์พื้นฐาน", "วิทยาการคำนวณ", "การออกแบบและเทคโนโลยี", "สังคมศึกษาฯ", "ประวัติศาสตร์", "สุขศึกษา", "พลศึกษา", "ศิลปะ", "การงานอาชีพ", "ภาษาอังกฤษพื้นฐาน"],
    "มัธยมศึกษาตอนปลาย": ["ภาษาไทยพื้นฐาน", "คณิตศาสตร์พื้นฐาน", "คณิตศาสตร์เพิ่มเติม", "ฟิสิกส์", "เคมี", "ชีววิทยา", "โลกและอวกาศ", "สังคมศึกษาฯ", "ประวัติศาสตร์", "สุขศึกษา", "พลศึกษา", "ศิลปะ", "การงานอาชีพ", "ภาษาอังกฤษพื้นฐาน", "ภาษาอังกฤษเพิ่มเติม", "วิทยาการคำนวณ", "การออกแบบและเทคโนโลยี"]
}

KPA_OPTIONS = {
    "K (Knowledge)": ["ความรู้ความเข้าใจในเนื้อหา", "การอธิบายหลักการ", "ความเข้าใจองค์ประกอบ"],
    "P (Process)": ["ทักษะการปฏิบัติ", "ทักษะการคิดวิเคราะห์", "ทักษะการทำงานกลุ่ม"],
    "A (Attitude)": ["ใฝ่เรียนรู้", "มุ่งมั่นในการทำงาน", "มีวินัย"]
}

# --- 2. การตกแต่ง UI (Theme สีฟ้า Azure Blue) ---
st.set_page_config(layout="wide", page_title="PLAN - ระบบช่วยเขียนแผนการสอน")

st.markdown("""
    <style>
    /* พื้นหลังแอป */
    .stApp { background-color: #f0f7ff; }
    
    /* ตกแต่ง Sidebar */
    [data-testid="stSidebar"] { 
        width: 480px !important; 
        background-color: #ffffff; 
        border-right: 4px solid #3498db;
    }

    /* หน้ากระดาษพรีวิว (Layer 1) */
    .paper-page {
        background-color: white; 
        padding: 50px 75px; 
        border: 1px solid #d1d1d1;
        box-shadow: 0 15px 35px rgba(52, 152, 219, 0.1); 
        color: #2c3e50; 
        line-height: 1.8;
        border-top: 10px solid #3498db; 
        border-radius: 4px;
    }

    /* หัวแผนการสอนในหน้า Preview */
    .plan-header { 
        text-align: center; 
        border-bottom: 2px solid #3498db; 
        margin-bottom: 30px; 
        padding-bottom: 10px; 
    }
    .plan-header h3 { color: #2980b9; margin-bottom: 5px; }

    /* ปุ่มกดสีฟ้า */
    div.stButton > button { 
        width: 100%; border-radius: 12px; height: 3.5em; 
        background-color: #3498db; color: #fff; font-weight: bold; border: none;
        transition: 0.3s; box-shadow: 0 4px 6px rgba(52, 152, 219, 0.2);
    }
    div.stButton > button:hover { background-color: #2980b9; transform: translateY(-2px); }

    /* สไตล์ตารางพรีวิว */
    table { width: 100%; border-collapse: collapse; margin: 15px 0; }
    th { background-color: #ebf5fb; color: #2980b9; border: 1px solid #3498db; padding: 10px; text-align: center; }
    td { border: 1px solid #ddd; padding: 10px; }
    </style>
    """, unsafe_allow_html=True)

# --- 3. ฟังก์ชัน Clean ข้อความ (สำคัญต่อการตั้งฟอนต์ใน Word) ---
def clean_content(text):
    # ลบสัญลักษณ์ตกแต่งทั้งหมด (#, *, _) เพื่อให้ Word ใช้ฟอร์แมตจาก Template ได้ 100%
    text = re.sub(r'[#*_]{1,3}', '', text)
    return text.strip()

# --- 4. การเชื่อมต่อ API ---
GOOGLE_API_KEY = st.secrets["GOOGLE_API_KEY"]
genai.configure(api_key=GOOGLE_API_KEY)
model = genai.GenerativeModel('gemini-2.5-flash')

# --- 5. SIDEBAR: ส่วนกรอกข้อมูล ---
with st.sidebar:
    st.title("💙 ข้อมูลแผนการจัดการเรียนรู้")
    with st.form("input_form"):
        school = st.text_input("โรงเรียน")
        teacher = st.text_input("ชื่อครูผู้สอน")
        term = st.text_input("ภาคเรียน/ปีการศึกษา", value="1 / 2567")
        
        grade_level = st.selectbox("ระดับการศึกษา", list(SUBJECTS_DATA.keys()))
        subject_name = st.selectbox("วิชา", SUBJECTS_DATA[grade_level])
        
        col1, col2 = st.columns(2)
        with col1:
            code = st.text_input("รหัสวิชา")
            grade = st.text_input("ชั้นปี (เช่น ม.3)")
        with col2:
            time_hours = st.text_input("เวลา (ชั่วโมง)", value="2")
            
        topic = st.text_input("หัวข้อบทเรียน")
        extra_skills = st.text_area("ทักษะที่ต้องการเน้นพิเศษ", height=80)
        
        sel_k = st.multiselect("ด้าน K", KPA_OPTIONS["K (Knowledge)"])
        sel_p = st.multiselect("ด้าน P", KPA_OPTIONS["P (Process)"])
        sel_a = st.multiselect("ด้าน A", KPA_OPTIONS["A (Attitude)"])
        
        submitted = st.form_submit_button("🚀 สร้างแผนการสอนฉบับสมบูรณ์")

# --- 6. ส่วนประมวลผล (Logic) ---
if submitted:
    with st.spinner('💎 AI กำลังออกแบบแผนการสอนสีฟ้าให้คุณ...'):
        prompt = f"""
        จงเขียนแผนการจัดการเรียนรู้ภาษาไทยโดยละเอียดสำหรับ วิชา {subject_name} ({code}) เรื่อง {topic} ชั้น {grade} 
        ห้ามใช้สัญลักษณ์ตกแต่งเช่น # หรือ * ให้ใช้การขึ้นบรรทัดใหม่และหัวข้อชัดเจน
        
        รายละเอียด:
        โรงเรียน: {school}, ผู้สอน: {teacher}, เวลา: {time_hours} ชม.
        ทักษะเน้นพิเศษ: {extra_skills}
        KPA: {sel_k}, {sel_p}, {sel_a}
        
        โครงสร้าง: มาตรฐานการเรียนรู้, สาระสำคัญ, สาระการเรียนรู้, จุดประสงค์ (K-P-A), สมรรถนะผู้เรียน, ชิ้นงาน, กิจกรรม (นำ/สอน/สรุป), สื่อ, การวัดผล, ตารางรูบริกส์ (4 ระดับ)
        """
        response = model.generate_content(prompt)
        
        # ป้องกัน KeyError ด้วยการสร้างค่าพร้อมกัน
        st.session_state['ai_raw'] = response.text
        st.session_state['data_context'] = {
            'school': school, 'teacher': teacher, 'term': term,
            'subject': subject_name, 'code': code, 'grade': grade,
            'topic': topic, 'time': time_hours
        }

# --- 7. ส่วนแสดงผลพรีวิวและ Export ---
if 'ai_raw' in st.session_state and 'data_context' in st.session_state:
    doc_data = st.session_state['data_context']
    
    # ส่วนดาวน์โหลด
    c1, c2 = st.columns([8, 2])
    with c2:
        try:
            doc = DocxTemplate("template.docx")
            # เนื้อหาที่ clean แล้วจะไปลงที่ตัวแปร {{ai_content}} ใน Word ซึ่งตั้งเป็นฟอนต์ 16 ปกติไว้
            final_data = {**doc_data, 'ai_content': clean_content(st.session_state['ai_raw'])}
            doc.render(final_data)
            
            bio = io.BytesIO()
            doc.save(bio)
            st.download_button("📘 โหลดไฟล์ Word", bio.getvalue(), f"แผน_{doc_data['topic']}.docx")
        except Exception as e:
            st.warning("⚠️ โปรดอัปโหลด template.docx และตรวจสอบชื่อตัวแปรให้ตรงกัน")

    # หน้ากระดาษพรีวิว
    st.markdown('<div class="paper-page">', unsafe_allow_html=True)
    st.markdown(f"""
        <div class="plan-header">
            <h3>บันทึกการจัดการเรียนรู้</h3>
            <p style="font-size: 1.1em; color: #2980b9;"><b>{doc_data['school']}</b></p>
            <p>ภาคเรียนที่ {doc_data['term']}</p>
            <p><b>หน่วยการเรียนรู้เรื่อง:</b> {doc_data['topic']}</p>
            <p>รหัสวิชา {doc_data['code']} ชื่อวิชา {doc_data['subject']} ชั้น {doc_data['grade']} เวลา {doc_data['time']} ชั่วโมง</p>
            <p><b>ผู้สอน:</b> {doc_data['teacher']}</p>
        </div>
    """, unsafe_allow_html=True)

    # พรีวิวเนื้อหา
    preview_text = clean_content(st.session_state['ai_raw'])
    st.markdown(preview_text)
    
    with st.expander("🛠️ แก้ไขเนื้อหาก่อนส่งออก"):
        st.session_state['ai_raw'] = st.text_area("แก้ไขที่นี่:", value=st.session_state['ai_raw'], height=450)
    st.markdown('</div>', unsafe_allow_html=True)
else:
    st.info("👋 ยินดีต้อนรับ! กรุณากรอกข้อมูลด้านซ้ายเพื่อสร้างแผนการจัดการเรียนรู้")