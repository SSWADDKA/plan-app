import streamlit as st
import google.generativeai as genai
from docxtpl import DocxTemplate
import io

# --- 1. ข้อมูลวิชาและตัวเลือกตามหลักสูตรไทย ---
# รวมวิชาหลักตามกลุ่มสาระการเรียนรู้
SUBJECTS_DATA = {
    "ประถมศึกษา": ["ภาษาไทย", "คณิตศาสตร์", "วิทยาศาสตร์และเทคโนโลยี", "สังคมศึกษาฯ", "สุขศึกษาและพลศึกษา", "ศิลปะ", "การงานอาชีพ", "ภาษาอังกฤษ"],
    "มัธยมศึกษาตอนต้น": ["ภาษาไทยพื้นฐาน", "คณิตศาสตร์พื้นฐาน", "วิทยาศาสตร์พื้นฐาน", "สังคมศึกษาฯ", "ประวัติศาสตร์", "สุขศึกษา", "พลศึกษา", "ศิลปะ", "การงานอาชีพ", "ภาษาอังกฤษพื้นฐาน", "วิทยาการคำนวณ"],
    "มัธยมศึกษาตอนปลาย": ["ภาษาไทยพื้นฐาน", "คณิตศาสตร์พื้นฐาน", "คณิตศาสตร์เพิ่มเติม", "ฟิสิกส์", "เคมี", "ชีวภาพ", "โลกและอวกาศ", "สังคมศึกษาฯ", "สุขศึกษา", "พลศึกษา", "ศิลปะ", "ภาษาอังกฤษพื้นฐาน", "ภาษาอังกฤษเพิ่มเติม", "วิทยาการคำนวณ", "การออกแบบและเทคโนโลยี"]
}

# ตัวอย่างตัวเลือก KPA อ้างอิงจากหลักสูตร
KPA_OPTIONS = {
    "K (Knowledge)": ["ความรู้ความเข้าใจในเนื้อหา", "ความสามารถในการอธิบาย", "ความเข้าใจหลักการและทฤษฎี", "ความจำและการระลึกได้"],
    "P (Process/Skill)": ["ทักษะการแก้ปัญหา", "ทักษะการคิดวิเคราะห์", "ทักษะการปฏิบัติงาน", "ทักษะการทำงานเป็นกลุ่ม", "ทักษะการสื่อสาร", "ทักษะทางเทคโนโลยี"],
    "A (Attitude/Attribute)": ["ความใฝ่เรียนรู้", "ความมุ่งมั่นในการทำงาน", "ความซื่อสัตย์สุจริต", "มีวินัย", "อยู่อย่างพอเพียง", "มีจิตสาธารณะ", "รักความเป็นไทย"]
}

# --- 2. การตั้งค่าหน้าจอและสไตล์ ---
st.set_page_config(layout="wide", page_title="PLAN - ระบบช่วยเขียนแผนการสอน")

st.markdown("""
    <style>
    .stApp { background-color: #fcfcfc; }
    [data-testid="stSidebar"] { width: 480px !important; background-color: #ffffff; border-right: 1px solid #eee; }
    .paper-container {
        background-color: #ffffff; padding: 45px 60px; border: 1px solid #e0e0e0;
        box-shadow: 0 10px 30px rgba(0,0,0,0.05); border-radius: 4px; color: #000000; margin-top: 20px;
    }
    div.stButton > button {
        width: 100%; border-radius: 8px; height: 3.5em; background-color: #000000;
        color: #ffffff; font-weight: bold; border: none;
    }
    </style>
    """, unsafe_allow_html=True)

# --- 3. การเชื่อมต่อ API ---
GOOGLE_API_KEY = st.secrets["GOOGLE_API_KEY"]
genai.configure(api_key=GOOGLE_API_KEY)
model = genai.GenerativeModel('gemini-2.5-flash')

# --- 4. SIDEBAR (ส่วนกรอกข้อมูลอัจฉริยะ) ---
with st.sidebar:
    st.markdown("### 📝 ข้อมูลแผนการจัดการเรียนรู้")
    with st.form("input_form"):
        school_name = st.text_input("โรงเรียน", placeholder="ระบุชื่อโรงเรียน")
        teacher_name = st.text_input("ชื่อครูผู้สอน")
        
        # ระบบ Filter ระดับชั้นและวิชา
        grade_level = st.selectbox("ระดับการศึกษา", ["ประถมศึกษา", "มัธยมศึกษาตอนต้น", "มัธยมศึกษาตอนปลาย"])
        
        # วิชาจะเปลี่ยนไปตามระดับการศึกษาที่เลือก
        available_subjects = SUBJECTS_DATA[grade_level]
        selected_subject = st.selectbox("รายวิชา", available_subjects)
        
        col1, col2 = st.columns(2)
        with col1:
            grade_spec = st.text_input("ชั้นปี (เช่น ป.5 หรือ ม.2)")
        with col2:
            duration = st.text_input("เวลา (ชั่วโมง)", placeholder="2")
            
        topic = st.text_input("หัวข้อ/เรื่องที่จะสอน", placeholder="ระบุหัวข้อบทเรียน")
        
        st.markdown("---")
        context_input = st.text_area("บริบทโรงเรียน/พื้นฐานนักเรียน", placeholder="เช่น นักเรียนมีความสนใจด้านเทคโนโลยี, โรงเรียนมีอุปกรณ์ครบ")
        
        st.markdown("**เลือกทักษะที่ต้องการเน้น (KPA)**")
        selected_k = st.multiselect("ด้านความรู้ (K)", KPA_OPTIONS["K (Knowledge)"])
        selected_p = st.multiselect("ด้านทักษะ (P)", KPA_OPTIONS["P (Process/Skill)"])
        selected_a = st.multiselect("ด้านเจตคติ/คุณลักษณะ (A)", KPA_OPTIONS["A (Attitude/Attribute)"])
        
        submitted = st.form_submit_button("🚀 สร้างแผนการสอน")

# --- 5. ส่วนประมวลผลหลัก ---
if submitted:
    with st.spinner('AI กำลังร่างแผนตามหลักสูตรแกนกลาง...'):
        k_str = ", ".join(selected_k)
        p_str = ", ".join(selected_p)
        a_str = ", ".join(selected_a)
        
        prompt = f"""
        จงเขียนแผนการจัดการเรียนรู้ภาษาไทยที่สมบูรณ์ตามหลักสูตรแกนกลางฯ ของประเทศไทย
        วิชา: {selected_subject} เรื่อง: {topic} ระดับชั้น: {grade_spec} ({grade_level})
        โรงเรียน: {school_name} ครูผู้สอน: {teacher_name} เวลา: {duration} ชั่วโมง
        
        ข้อมูลเพิ่มเติม:
        - บริบทโรงเรียน: {context_input}
        - จุดประสงค์ที่ต้องการเน้น:
            K: {k_str}
            P: {p_str}
            A: {a_str}
        
        จงเขียนเนื้อหาแยกตามโครงสร้างมาตรฐาน [STANDARDS], [CONCEPT], [OBJECTIVES], [ACTIVITIES], [RESOURCES], [EVALUATION]
        โดยในส่วนกิจกรรม (ACTIVITIES) ให้ใช้รูปแบบการสอนเชิงรุก (Active Learning) เช่น 5E หรือ GPAS 5 Steps
        """
        response = model.generate_content(prompt)
        full_text = response.text
        
        def get_section(tag, text):
            try:
                start = text.find(f"[{tag}]") + len(tag) + 2
                end = text.find("[", start)
                return text[start:end].strip() if end != -1 else text[start:].strip()
            except: return ""

        st.session_state['plan_data'] = {
            'school_name': school_name, 'teacher_name': teacher_name,
            'subject': selected_subject, 'grade': grade_spec, 'topic': topic, 'duration': duration,
            'standards': get_section("STANDARDS", full_text),
            'concept': get_section("CONCEPT", full_text),
            'objectives': get_section("OBJECTIVES", full_text),
            'activities': get_section("ACTIVITIES", full_text),
            'resources': get_section("RESOURCES", full_text),
            'evaluation': get_section("EVALUATION", full_text),
            'full_display': full_text
        }

# --- 6. ส่วนแสดงผลบนหน้าเว็บ ---
if 'plan_data' in st.session_state:
    data = st.session_state['plan_data']
    
    col_title, col_btn = st.columns([7, 3])
    with col_btn:
        try:
            doc = DocxTemplate("template.docx")
            doc.render(data)
            bio = io.BytesIO()
            doc.save(bio)
            st.download_button("📥 ดาวน์โหลดไฟล์ Word (.docx)", bio.getvalue(), f"แผนการสอน_{data['topic']}.docx")
        except:
            st.warning("⚠️ อย่าลืมอัปโหลด template.docx ขึ้น GitHub")

    st.markdown('<div class="paper-container">', unsafe_allow_html=True)
    st.markdown(f"<h2 style='text-align:center;'>แผนการจัดการเรียนรู้</h2>", unsafe_allow_html=True)
    st.markdown(f"<p style='text-align:center;'><b>วิชา:</b> {data['subject']} | <b>เรื่อง:</b> {data['topic']} | <b>ชั้น:</b> {data['grade']}</p>", unsafe_allow_html=True)
    
    edited_text = st.text_area("เนื้อหาแผนการสอน (แก้ไขได้ที่นี่):", value=data['full_display'], height=800)
    
    if edited_text != data['full_display']:
        st.session_state['plan_data']['full_display'] = edited_text
        st.session_state['plan_data']['activities'] = edited_text
        
    st.markdown('</div>', unsafe_allow_html=True)